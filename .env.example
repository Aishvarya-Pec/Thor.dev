# Database (SQLite - no setup required)
DATABASE_URL="file:./dev.db"

# NextAuth Configuration
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="thor-dev-local-secret-key-for-development-only"

# Development Settings
NODE_ENV="development"
PORT=3000

# WebSocket Server
WS_PORT=8080
WS_HOST="localhost"

# Optional: Add your own keys if you want real integrations
# Leave empty to use mock implementations

# OAuth Providers (Optional - uses guest mode if empty)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# GitHub Integration (Optional - uses local storage if empty)
GITHUB_TOKEN=""

# Deployment Services (Optional - generates static files if empty)
VERCEL_TOKEN=""
NETLIFY_TOKEN=""

# Email Configuration (Optional - uses console logging if empty)
EMAIL_SERVER_HOST=""
EMAIL_SERVER_PORT=""
EMAIL_SERVER_USER=""
EMAIL_SERVER_PASSWORD=""
EMAIL_FROM="noreply@thor.dev"

# AI Services (All optional - uses smart fallbacks)

# Ollama (Local AI - Recommended for privacy and cost)
OLLAMA_BASE_URL="http://localhost:11434"
OLLAMA_MODEL="codellama:7b"

# Hugging Face (Free tier available)
HUGGINGFACE_API_KEY=""

# Cohere (Free tier available)  
COHERE_API_KEY=""

# OpenAI (Paid service)
OPENAI_API_KEY=""

# Anthropic (Paid service)
ANTHROPIC_API_KEY=""

# File Storage (Uses local filesystem by default)
AWS_ACCESS_KEY_ID=""
AWS_SECRET_ACCESS_KEY=""
AWS_REGION=""
AWS_S3_BUCKET=""